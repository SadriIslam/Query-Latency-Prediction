Results chart is generated automatically when you run:

    cd ml
    python ml_model.py

The chart will be saved here as results_chart.png
It shows:
  - MAE comparison (all 4 models)
  - RMSE comparison (all 4 models)
  - Feature importance (Gradient Boosting)
  - Actual vs Predicted scatter plot (Gradient Boosting)
